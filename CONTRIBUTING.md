# ร่วมพัฒนา

repo นี้มีโปรเจกต์อื่นดึง `/model/info` ไปตัดสินใจเลือกโมเดล ตัวเลขที่ผิดจึงไม่ได้
ทำให้แค่ repo นี้เสีย แต่ทำให้ agent ของคนอื่นพังกลางบทสนทนา กติกาข้างล่างมีไว้
เพื่อกันเรื่องนั้น ไม่ได้มีไว้ให้ครบพิธี

## กติกาข้อเดียวที่สำคัญที่สุด

> **ทุกอย่างที่วัดค่าต้องส่ง `disable_fallbacks` · ทุกอย่างที่อ่านค่าต้องเช็ค `answered_by`**

LiteLLM มี `fallbacks` ที่สลับไป provider อื่นให้อัตโนมัติเมื่อตัวหลักตาย ซึ่งดีตอน
ใช้งานจริง แต่พังตอนวัด — เพราะเราจะวัดตัวสำรองแล้วบันทึกผลใส่ชื่อโมเดลที่ขอ

รากเดียวกันนี้ทำให้พลาดมาแล้ว **5 ครั้งใน 2 วัน** (`probe-context.py`,
`probe-latency-14k.py`, ตัวกรองใน `pick-model.sh`, `bench-coding.py`,
`verify-capabilities.py`) รายละเอียดอยู่ใน [INTEGRATION.md](INTEGRATION.md)

`scripts/validate.sh` ตรวจข้อนี้ให้อัตโนมัติแล้ว — โมเดลที่มี `answered_by`
ต้องไม่มีฟิลด์วัดผลติดอยู่

## อยากใช้โมเดลใหม่

เปิด issue มาได้เลย มีสองทาง:

1. **ให้เราทดสอบให้** — บอกชื่อโมเดลกับ provider พอ
2. **ทดสอบเองแล้วส่ง PR มา** — เร็วกว่า และคุณเห็นสภาพจริงในบริบทของคุณมากกว่าเรา

ทางที่ 2 ขอให้ใส่ `verified_by` กำกับด้วยว่าใครวัด วัดเมื่อไหร่ วัดยังไง:

```yaml
verified_by: "hermes-line-bot 2026-08-26 (tool loop + prompt 14K)"
```

ผลจากการใช้งานจริงมีน้ำหนักกว่าผลที่เรายิง API ตรงเสมอ เพราะมีของที่ gateway
วัดให้ไม่ได้ — เช่นโมเดลที่ตอบภาษาอื่นเฉพาะตอนอยู่ใน tool loop ที่มี system
prompt ยาว ๆ ดูตาราง "gateway วัดอะไรให้ไม่ได้" ใน [INTEGRATION.md](INTEGRATION.md)

## ก่อนส่ง PR

```bash
./scripts/validate.sh      # ตรวจ config + compose + Caddyfile + secret + fallback
pytest tests/ -q           # ถ้าแตะโค้ด Python
ruff check scripts/ tests/
```

CI รันชุดเดียวกันนี้ ถ้าผ่านในเครื่องก็ผ่านบน GitHub

## แก้ค่าใน `model_info`

อย่าแก้ `litellm/config.yaml` ด้วยมือถ้าเป็นฟิลด์ที่สคริปต์เขียน — ใช้
`scripts/config_edit.py` ผ่านสคริปต์วัดผลแทน

เหตุผล: ตัวเขียนรุ่นก่อนใช้ regex แล้วสร้าง key ซ้ำทั้ง 115 โมเดลโดยไม่มีอะไรฟ้อง
YAML เลือก key อันท้ายสุดเงียบ ๆ ทำให้ค่าที่เพิ่งวัดไม่มีผลเลย

ฟิลด์ที่สคริปต์เป็นเจ้าของ: `status` `status_checked_at` `status_detail`
`answered_by` `verified_max_prompt` `max_prompt_detail` `latency_ms_14k`
`benchmark_coding` `benchmark_seconds`

## restart gateway

**แจ้งใน issue ก่อนเสมอ** — มีบอทและ agent ของทีมอื่นต่ออยู่ การ restart
ระหว่างมีคนคุยกับบอททำให้ข้อความหาย

## เขียนโค้ดในนี้

- คอมเมนต์บอก **ทำไม** ไม่ใช่ **ทำอะไร** — โค้ดบอกอยู่แล้วว่าทำอะไร
- เจอบั๊กแล้วแก้ ให้เขียนไว้ด้วยว่าเดิมพังยังไง คนถัดไปจะได้ไม่ทำซ้ำ
- ตรรกะที่ใช้ร่วมกันให้แยกเป็นโมดูล อย่าลอกไปวาง — `failure_hints.py` กับ
  `config_edit.py` เกิดขึ้นเพราะโค้ดที่ลอกกันไปแล้วเพี้ยนจนสรุปผิด
